# 安全策略

## 支持范围

安全修复优先应用于默认分支的最新版本。请先确认问题在最新版本仍可复现。

## 报告漏洞

请通过 GitHub 仓库的 **Security → Report a vulnerability** 私下报告安全问题；在修复发布前，请不要创建公开 Issue。

报告中请包含：受影响版本、复现步骤、可能影响，以及可用于验证修复的最小示例。请勿附带真实用户的轨迹、精确住址或其他敏感位置数据；如必须提供，请先脱敏。

本项目不会要求报告者运行来源不明的程序，也不会索取密码、令牌或账户凭据。

## 本地数据边界

GPX 文件在本机解析，不会上传。地图瓦片由 OpenStreetMap 加载；只有用户主动执行地点搜索时，搜索词才会发送给 Photon。FIT 文件仅写入用户选择的输出目录。
